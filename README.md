# INTRVW

An AI-powered software engineering mock-interview platform.
I would like an application that allows users to mock interview with a small browser based AI.
It should use Trillim `uv add trillim` as the model so that everything can run in the browser, not on the backend. 
Also, it should support TTS and STT so the user can speak and be spoken to.
